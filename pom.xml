<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.camunda</groupId>
		<artifactId>camunda-release-parent</artifactId>
		<version>2.5</version>
		<relativePath />
  	</parent>
  
	<groupId>com.camunda.consulting</groupId>
	<artifactId>camunda-tngp-monitor</artifactId>
	<version>1.0.0-SNAPSHOT</version>

	<packaging>war</packaging>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<tngp.version>1.0.0-alpha1</tngp.version>
		<version.java>1.8</version.java>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
        <failOnMissingWebXml>false</failOnMissingWebXml>
    </properties>

	<dependencies>
		<dependency>
			<groupId>org.camunda.tngp</groupId>
			<artifactId>tngp-client-java</artifactId>
			<version>${tngp.version}</version>
		</dependency>

		<!-- Dependency to allow starting the broker embedded -->
		<dependency>
			<groupId>org.camunda.tngp</groupId>
			<artifactId>tngp-broker-core</artifactId>
			<version>${tngp.version}</version>
		</dependency>

		<dependency>
			<!-- Bootstrap for styling via Webjars project -->
			<groupId>org.webjars</groupId>
			<artifactId>bootstrap</artifactId>
			<version>2.3.2</version>
		</dependency>
<!-- 
	<dependency>
 <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter</artifactId>
  <version>1.4.0.RELEASE</version>
</dependency>  
 -->
		<dependency>
			<!-- Java EE 6 APIs -->
			<groupId>javax</groupId>
			<artifactId>javaee-api</artifactId>
			<version>6.0</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
		    <groupId>org.webjars.bower</groupId>
		    <artifactId>bpmn-js</artifactId>
		    <version>0.16.0</version>
		</dependency>
	</dependencies>

	<build>
		<finalName>${project.artifactId}</finalName>
		<plugins>
			<plugin>
				<!-- Usage: mvn wildfly:deploy See also: https://docs.jboss.org/wildfly/plugins/maven/latest/examples/deployment-example.html -->
				<groupId>org.wildfly.plugins</groupId>
				<artifactId>wildfly-maven-plugin</artifactId>
				<version>1.0.2.Final</version>
			</plugin>
		</plugins>
	</build>
	<repositories>
		<repository>
			<id>camunda-private</id>
			<url>https://app.camunda.com/nexus/content/groups/private/</url>
		</repository>
	</repositories>

	<scm>
		<url>https://github.com/camunda-consulting/camunda-tngp-monitor</url>
		<connection>scm:git:git@github.com:camunda-consulting/camunda-tngp-monitor.git</connection>
		<developerConnection>scm:git:git@github.com:camunda-consulting/camunda-tngp-monitor.git</developerConnection>
		<tag>HEAD</tag>
	</scm>

	<distributionManagement>
		<snapshotRepository>
			<id>camunda-nexus</id>
			<url>https://app.camunda.com/nexus/content/repositories/camunda-consulting-private-snapshots/</url>
		</snapshotRepository>
		<repository>
			<id>camunda-nexus</id>
			<url>https://app.camunda.com/nexus/content/repositories/camunda-consulting-private/</url>
		</repository>
	</distributionManagement>	
</project>